package EserciziIteratori;

import java.util.List;
/*
public class Triangle extends Polygon{
    public Triangle(Point p1, Point p2, Point p3){
        super(List.of(p1,p2,p3));

    }
    @Override
    public double area(){
        Point p1 = points.get(0);
        Point p2 = points.get(1);
        if(p1.x == p2.x){
            double base = new Line(p1,p2).length();
            /*Point px = new Point(p2.x, p1.y);
            double h=new Line(px, p2).length();
            */
        /*
            double h=new Line(new Point(p2.x, p1.y), p2).length();
            return base *h/2.;
        }else{
            //formula di Erone
            Point p3 = points.get(2);
            double p = perimeter() /2.;
            double a = new Line(p1,p2).length();
            double b= new Line(p2,p3).length();
            double c= new Line(p3,p1).length();

            return Math.sqrt(p * (p -a) * (p-b) * (p-c));

        }
    }
}
*/